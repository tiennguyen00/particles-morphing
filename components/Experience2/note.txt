
uRenderMode = 2
uSource = 0, -0.5, 0
using initPos as the renderTarget
sceneFBO includes the geometry points (-0.5 -> 0.5, z = 0)

default uCurrentPosition = getPointsOnSphere()
default uDirections = null
==> return value: 0 +- 0.1, -0.5 +- 0.1, 0 +- 0.1


Now,
uCurrentPosition = the returned above value.

2.
uDirections = null
uRenderMode = 0
setDrawRange(0, number)
-> swithc to renderTarget

3.
setDrawRange(currentParticles, emit)
gl.autoClear = false

uRenderMode = 1
uDirections = null
uSource = emitterDir
==> return value direction: gl_FragColor = vec4( uSource + vec3(rnd1,rnd2,rnd3) * 0.2, uTime);

4.
uRenderMode = 2
uCurrentPosition = null
uSource = emitter.current.position
==> return value position: gl_FragColor = vec4( uSource + vec3(rnd1,rnd2,rnd3)*0.2, 1.);

gl.autoClear = true


